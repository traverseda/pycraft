#Very simple settings to start with.
#We'll expand when we need more features, like changing settings via a gui

#You can tell git to ignore your local changes to this file with
#git update-index --assume-unchanged pycraft/settings.py

core = {
    'tickrate':60
}

graphics = {
    'fps':60,
    'shaders':True,
}

controls = {

}
